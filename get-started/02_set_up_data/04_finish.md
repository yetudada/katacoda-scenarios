We use `kedro ipython` in this example but you can use `kedro jupyter notebook` to explore and profile datasets.

Let's go over to the next stage. Building a simple data pipeline to process this `titanic` set.